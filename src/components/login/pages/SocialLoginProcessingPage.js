import React from "react";
import Loading from "../../layout/loading/LoadingMessage";

const SocialLoginProcessingPage = (props) => {
    return(
        <Loading/>
    )
};

export default SocialLoginProcessingPage;